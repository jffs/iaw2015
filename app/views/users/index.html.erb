
<titulo> Usuarios registrados</titulo><br/><br/>
<table class="table">

	<tr>
		<th><thtabla> Usuario </thtabla></th>
		<th><thtabla> Categoria</thtabla></th>
		<th><thtabla> Foto </thtabla></th>
		<th><thtabla> Acciones </thtabla></th>
		<th><%= link_to "Registrarse" , new_user_path(), class: 'btn btn-info btn-danger'%></th>
	</tr>

	<% usuarios = @usuario%>
	<% usuarios.each do |usuario| %>
		<tr>
			
			<td> <%= link_to usuario.nick, usuario %> </td>
			<td> <%= usuario.role %> </td>
			<td><%= image_tag usuario.foto, :width => 128, :height => 128 %> </td> 
			<td>
				<%= link_to "Editar", edit_user_path(usuario) ,  class: 'btn btn-info btn-sm'%>
				<%= link_to "Eliminar", destroy_user_path(usuario), data: { confirm: 'Estás seguro?' },  class: 'btn btn-danger btn-sm'%>
			</td>
		</tr>
	<% end %>

	<% if User.all.size == 0 %>
		<tr> <td colspan="3"> No hay usuarios registrados </td> </tr>
	<% end %>
</table>
<CENTER><%= will_paginate @usuario ,  renderer: BootstrapPagination::Rails %></CENTER>